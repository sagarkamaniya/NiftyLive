<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.niftylive">

    <!-- ✅ Internet permission (required for SmartAPI / Retrofit calls) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: Keep screen on during live data -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".NiftyLiveApp"  <!-- ✅ Required for Hilt -->
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NiftyLive"
        android:exported="true">

        <!-- ✅ Your main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- (Optional) For splash screen if added later -->
        <!--
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.NiftyLive.Splash"/>
        -->

    </application>

</manifest>